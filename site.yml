---

# masters up first, since they
# should be upgraded before slaves

- hosts: masters
  sudo: yes
  roles:
    - log/sink
    - mesos/master

- hosts: slaves
  sudo: yes
  roles:
    - log/fwd
    - mesos/slave

- hosts: zookeepers
  sudo: yes
  roles:
    - zookeeper
